name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20.19.0"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Syntax-check service worker, shared module, and page scripts
        run: |
          node --check background.js
          node --check domain.js
          node --check options.js
          node --check popup.js

      - name: Run unit tests
        run: npm test

      - name: Validate manifest
        run: node scripts/validate-manifest.mjs

      - name: Assemble extension bundle
        run: node scripts/package.mjs --assemble-only

      - name: Lint extension (Chrome and Edge relevant errors only)
        run: node scripts/check-lint.mjs dist/ext
